;(function(lw) {

    /**
    * LaserWeb about module.
    *
    * Description...
    */
    lw.add_module('layout.panes.about', {

        // Autoload the module ?
        autoload: true,

        // Module version
        version: '0.0.1',

        // Module icon
        icon: 'question',

        // Extends
        extends: ['layout.pane']

    });

})(laserweb);
