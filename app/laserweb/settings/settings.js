;(function(lw) {

    /**
    * LaserWeb settings module.
    *
    * Description...
    */
    lw.add_module('settings', {

        // Autoload the module ?
        autoload: true,

        // Module version
        version: '0.0.1',

        // Module icon
        icon: 'cogs'

    });

})(laserweb);
